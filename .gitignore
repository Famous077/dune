node_modules/
dist/
*.tsbuildinfo
.claude
# SAM build output
.aws-sam/

# embedding model files, fetched at build time and bundled into the Lambda
.models/

# never commit env files — the parser skips them and so does the repo
.env
.env.*
!.env.example

# local AWS / editor noise
.DS_Store
*.log
